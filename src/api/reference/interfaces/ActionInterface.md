---
title: "ActionInterface"
editLink: false
# Generated by `npm run sync:docs` from TypeDoc output in the kist
# repository. Edits here are overwritten; change the source doc comment.
---

[**kist**](/api/reference/)

***

[kist](../globals) / ActionInterface

# Interface: ActionInterface

Defined in: [src/ts/interface/ActionInterface.ts:17](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/interface/ActionInterface.ts#L17)

ActionInterface defines the contract for step action classes in kist.
Implementing this interface allows actions to be dynamically integrated into
the pipeline, offering a flexible and extensible system for defining custom
behaviors within different stages.

## Properties

### name

> **name**: `string`

Defined in: [src/ts/interface/ActionInterface.ts:23](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/interface/ActionInterface.ts#L23)

A unique name identifying the action.
This property is used for logging, registration in the ActionRegistry,
and debugging purposes.

## Methods

### execute()

> **execute**(`options`): `Promise`\<`void`\>

Defined in: [src/ts/interface/ActionInterface.ts:39](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/interface/ActionInterface.ts#L39)

Executes the action with the provided options.
Implementations of this method should perform the main logic of the
action, such as building, testing, linting, packaging, or other
custom-defined processes.

#### Parameters

##### options

[`ActionOptionsType`](ActionOptionsType)

A structured set of options specific to the action's
configuration. Implementations are responsible for validating and
applying these options. Example configurations might include:
- For 'build': `{ minify: boolean, sourceMap: boolean, target: string }`
- For 'lint': `{ fix: boolean, formatter: string }`

#### Returns

`Promise`\<`void`\>

A Promise that resolves when the action completes successfully,
or rejects with an error if the action fails.

***

### validateOptions()?

> `optional` **validateOptions**(`options`): `boolean`

Defined in: [src/ts/interface/ActionInterface.ts:51](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/interface/ActionInterface.ts#L51)

Validates the provided options before execution.
This method can be used to check the integrity and correctness of the
options object, ensuring that required fields are present and valid.

#### Parameters

##### options

[`ActionOptionsType`](ActionOptionsType)

The options to validate, ensuring they meet the
action's specific requirements.

#### Returns

`boolean`

A boolean indicating whether the options are valid. Throws an
error or returns false if validation fails.

***

### describe()?

> `optional` **describe**(): `string`

Defined in: [src/ts/interface/ActionInterface.ts:60](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/interface/ActionInterface.ts#L60)

Provides a summary or description of the action.
This can be used for logging, debugging, or documentation purposes to
describe what the action does.

#### Returns

`string`

A string description of the action.

***

### cleanup()?

> `optional` **cleanup**(): `Promise`\<`void`\>

Defined in: [src/ts/interface/ActionInterface.ts:69](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/interface/ActionInterface.ts#L69)

Optional cleanup logic to execute after the main action completes.
Implementations can use this for teardown tasks, like removing
temporary files or restoring the state altered during execution.

#### Returns

`Promise`\<`void`\>

A promise that resolves after cleanup is complete.
