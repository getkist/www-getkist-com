---
title: "Kist"
editLink: false
# Generated by `npm run sync:docs` from TypeDoc output in the kist
# repository. Edits here are overwritten; change the source doc comment.
---

[**kist**](/api/reference/)

***

[kist](../globals) / Kist

# Class: Kist

Defined in: [src/ts/kist.ts:22](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/kist.ts#L22)

The Kist class encapsulates the kist CLI functionality.
It manages the pipeline execution, configuration loading, and live reload.

## Extends

- `AbstractProcess`

## Constructors

### Constructor

> **new Kist**(): `Kist`

Defined in: [src/ts/kist.ts:29](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/kist.ts#L29)

Constructs the Kist class instance and initializes necessary components.

#### Returns

`Kist`

#### Overrides

`AbstractProcess.constructor`

## Properties

### logger

> `protected` `readonly` **logger**: `Logger`

Defined in: [src/ts/core/abstract/AbstractProcess.ts:23](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/core/abstract/AbstractProcess.ts#L23)

Logger instance for handling log messages.

#### Inherited from

`AbstractProcess.logger`

## Methods

### logInfo()

> `protected` **logInfo**(`message`): `void`

Defined in: [src/ts/core/abstract/AbstractProcess.ts:45](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/core/abstract/AbstractProcess.ts#L45)

Logs an informational message with the originating class name as context.
Use this for standard informational messages.

#### Parameters

##### message

`string`

The message to log.

#### Returns

`void`

#### Inherited from

`AbstractProcess.logInfo`

***

### logDebug()

> `protected` **logDebug**(`message`): `void`

Defined in: [src/ts/core/abstract/AbstractProcess.ts:53](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/core/abstract/AbstractProcess.ts#L53)

Logs a debug message with the originating class name as context.

#### Parameters

##### message

`string`

The debug message to log.

#### Returns

`void`

#### Inherited from

`AbstractProcess.logDebug`

***

### logWarn()

> `protected` **logWarn**(`message`): `void`

Defined in: [src/ts/core/abstract/AbstractProcess.ts:62](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/core/abstract/AbstractProcess.ts#L62)

Logs a warning message with the originating class name as context.
Use this to highlight potential issues that are non-critical.

#### Parameters

##### message

`string`

The warning message to log.

#### Returns

`void`

#### Inherited from

`AbstractProcess.logWarn`

***

### logError()

> `protected` **logError**(`message`, `error?`): `void`

Defined in: [src/ts/core/abstract/AbstractProcess.ts:76](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/core/abstract/AbstractProcess.ts#L76)

Logs an error message with the originating class name as context.
Handles any type of error and ensures consistent error reporting.
Use this for logging critical issues or exceptions.

#### Parameters

##### message

`string`

A custom message providing additional context for
the error.

##### error?

`unknown`

(Optional) The error to log. Can be a string, an Error
object, or other types.

#### Returns

`void`

#### Inherited from

`AbstractProcess.logError`

***

### logSuccess()

> `protected` **logSuccess**(`message`): `void`

Defined in: [src/ts/core/abstract/AbstractProcess.ts:106](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/core/abstract/AbstractProcess.ts#L106)

Logs a success message with the originating class name as context.
Use this to indicate successful completion of a process or step.

#### Parameters

##### message

`string`

The success message to log.

#### Returns

`void`

#### Inherited from

`AbstractProcess.logSuccess`

***

### run()

> **run**(): `Promise`\<`void`\>

Defined in: [src/ts/kist.ts:50](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/kist.ts#L50)

Executes the Kist workflow.

This method orchestrates the execution of the Kist pipeline, starting
from initializing the ActionRegistry, loading configuration settings,
running the pipeline stages through the `PipelineManager`, and
optionally enabling live reload for real-time updates.

#### Returns

`Promise`\<`void`\>

Resolves when the workflow completes successfully.

#### Example

```ts
const Kist = new Kist();
Kist.run().then(() => console.log("Pipeline execution complete."));
```

***

### prepare()

> **prepare**(): `Promise`\<`void`\>

Defined in: [src/ts/kist.ts:98](https://github.com/getkist/kist/blob/f71ef15f65fa7d6bf20184980805f739271c031f/src/ts/kist.ts#L98)

Prepares the workflow without running it: discovers plugins, registers
actions, and validates the merged configuration.

Separated from [run](#run) so the CLI's inspection commands
(`--dry-run`, `--graph`, `validate`) can report on a fully resolved
configuration without executing a single step.

#### Returns

`Promise`\<`void`\>

#### Throws

Error if the configuration is invalid.
